{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development nodemon --watch ./ --exec babel-node -- index.js",
    "build": "rimraf ../dist && babel ./ --out-dir ../dist/ --ignore ./node_modules, ./.babelrc, ./.package.json, ./npm-debug.log, -d ../dist &&./models/config.json dist/models/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2017": "^6.24.1",
    "compression": "^1.7.1",
    "cookie-parser": "^1.4.3",
    "express": "^4.16.2",
    "express-device": "^0.4.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-2017": "0.0.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "eslint": "^4.13.1",
    "nodemon": "^1.14.1",
    "rimraf": "^2.6.2"
  }
}
